# Reflections

This directory stores agent-local reflection config and JSONL reflection logs that Foundry can read through `/api/reflections`.

The example agent only ships the directory skeleton for now.
